{
    "buildCommand": "cd client && npm run build",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/client/(.*)",
            "destination": "/client/index.html"
        }
    ]
}